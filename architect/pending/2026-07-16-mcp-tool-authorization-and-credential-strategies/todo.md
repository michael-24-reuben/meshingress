# Todo

- [ ] Define the initial scope-to-action and scope-to-tool mapping.
- [ ] Filter `tools/list` and enforce `tools/call` with one decision service.
- [ ] Add stable `authorization_denied` and credential-selection failure outcomes.
- [ ] Inventory every upstream integration and declare exactly one credential strategy.
- [ ] Implement `none` and `secret-reference` first; add exchange/delegated/workload adapters only with their persistence and revocation contracts.
- [ ] Add decision/audit/redaction integration tests.
