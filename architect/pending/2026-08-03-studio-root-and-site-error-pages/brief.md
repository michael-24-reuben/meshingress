# Studio Root and Site Error Pages

Make `/` the entry point to Meshingress Workflow Studio. Replace the generic Spring fallback at `/error` with a deliberate error surface that belongs to the same site.

This is planning only. No routing, build integration, static assets, API errors, or Studio code are changed by this record.
