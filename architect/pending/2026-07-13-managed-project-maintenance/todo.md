# Todo

- [ ] Inventory wrapped vendors and distinguish them from Maven-managed dependencies.
- [ ] Decide the final lock-manifest location and schema-validation approach.
- [ ] Define what code-review inspections are available without adding unapproved tooling.
- [ ] Design the `-Check` report format and exit-code contract.
- [ ] Implement and test the read-only check mode.
- [ ] Add build/dependency validation without version mutation.
- [ ] Add protected asset repair only after lock-manifest validation is complete.
- [ ] Remove the stale Mockito maintenance/download scripts and document the Maven replacement.
