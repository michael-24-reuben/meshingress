# Todo

- [x] Record the versioned-workspace decision.
- [x] Record explicit per-read delegation as the only prior-version fallback mechanism.
- [ ] Define the public `ToolWorkspace` contract and dependency/module ownership.
- [ ] Decide how a runtime-loaded class is mapped to its authoritative artifact coordinate.
- [ ] Choose the explicit delegation API and compatible-version selector.
- [ ] Define sensitive-resource policy and explicit migration behavior.
- [ ] Implement workspace creation, injection, containment checks, and lifecycle handling.
- [ ] Add focused unit and runtime-loader integration coverage.
- [ ] Adopt the API in a first tool only after the generic foundation is verified.
