# Todo

- [ ] Define the compatibility and migration behavior for `LOCAL_ASYNC_EXTERNAL`, `DELEGATED_EXTERNAL`, and `EXTERNAL_EXTERNAL` configuration values.
- [ ] Add a per-workspace/tool transfer contract with `LOCAL_BYTES` and `DELEGATED_SOURCE_URLS`.
- [ ] Restrict asynchronous Meshingress handoff to local-byte workspaces.
- [ ] Register local and delegated storage implementations concurrently instead of selecting one globally at startup.
- [ ] Update Toonverse to request delegated transfer explicitly.
- [ ] Define qBittorrent completion and local-file collection boundaries before adding that module.
- [ ] Add focused unit, MVC, and integration coverage for both transfer paths and their status semantics.
- [ ] Document the final property, tool-SPI, and status contracts.
