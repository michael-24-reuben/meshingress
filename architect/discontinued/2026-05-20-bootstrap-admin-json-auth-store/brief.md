# Brief

Bootstrap MCP admin authentication with first-admin registration and JSON-backed storage, with a planned migration path to SQL. Replace the previous property-based auth and keep the store pluggable.

