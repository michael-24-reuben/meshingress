# Brief

Replace the fragile delegated Nextcloud source-import lifecycle with a clean, Meshingress-owned streaming upload path. Preserve the deployed app until the replacement has passed a real book-render acceptance test.

