# Todo

- [x] Add the attachable `toolspace/x-transform` Maven module and server bundle dependency.
- [x] Add enum-backed transform selection and the `list-types` function.
- [x] Add `text.code` typed structured content and focused dispatch coverage.
- [x] Extract the initial backend-only Node bridge and remove all UI source from the new module.
- [ ] Port and verify every remaining conversion route from the upstream Transform UI before resolving this record.
- [ ] Add an application-level MCP discovery and `tools/call` test after all routes are verified.
