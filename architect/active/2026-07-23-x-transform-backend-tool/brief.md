# X Transform Backend Tool

Create an `x-transform` Meshingress tool from `ritz078/transform`, removing the web UI and exposing one transform-dispatch function plus one transform-type-list function.

The dispatch result must use structured `text.code` content, with an output-language hint, rather than plain-text structured content.
