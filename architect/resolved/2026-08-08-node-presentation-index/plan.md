# Plan

1. Track module ownership for classpath and runtime handlers and expose optional `moduleToolId` in MCP `tools/list` output.
2. Build one Studio presentation index from functions and module summaries.
3. Replace `ToolNodeIcon` identity inference with descriptor rendering and use it in the Explorer, canvas, inspector, and runtime drawer.
4. Verify server ownership output and the Studio production build.
