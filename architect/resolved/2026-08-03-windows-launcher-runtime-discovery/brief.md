# Windows Launcher Runtime Discovery

Make the Windows Meshingress control center recognize a healthy local Meshingress process started through `spring-boot:run` when the launcher state file is absent. Discovery must not classify unrelated Java processes as Meshingress.
