# Notes

- The future MCP names are intentionally role-gated names, but no route is
  installed in this implementation slice.
- Raw secret operations remain a host secret-store concern; Aegis only manages
  safe references and their lifecycle metadata.
