# Notes

The active root assignment concerns Nextcloud deployment and must remain untouched. This entry is limited to `app/meshingress-studio-web` and architect record files.

The browser-recents cache persists only root/file labels and relative paths in local storage. It does not persist file handles, enumerate directories again after reload, or send data to the server.
