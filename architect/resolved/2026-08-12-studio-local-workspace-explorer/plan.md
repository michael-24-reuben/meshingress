# Plan

1. Add the Explorer rail item and an Explorer-only body that renders the active local directory tree.
2. Wire File > New workspace, Open workspace, and Open recent to browser-local workspace selection and cache state.
3. Keep file clicks inert for Studio rendering and verify lint/build plus the local panel interaction.
