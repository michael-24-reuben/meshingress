# Studio Local Workspace Explorer

Add an Explorer item to the top-left Studio activity rail. It exposes the user-selected local workspace in the left panel and connects File menu workspace commands to client-only selection flows.

This slice must not send local paths or file data to the server, render Studio files when they are clicked, or write a Studio workspace.
