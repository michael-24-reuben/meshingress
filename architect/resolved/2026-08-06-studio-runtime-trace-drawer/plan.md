# Plan

1. Capture WebSocket node lifecycle times in Studio state and assign a lane only while an execution interval overlaps another active interval.
2. Add the Runtime drawer tab, toolbar, collapsible filters, timeline, table, and footer while preserving Current execution, Variables, and Definition.
3. Make clear action session-local and keep Stop visibly unavailable until a runtime cancellation API exists.
4. Verify TypeScript/lint/build and inspect the live Studio layout.
