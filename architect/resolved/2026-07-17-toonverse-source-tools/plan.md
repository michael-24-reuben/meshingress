# Plan

1. Add source-owned Toonverse request contracts and an annotated `toonverse` tool with metadata, paged-book, chapter-reader, and search functions.
2. Keep search query construction and filter validation in the Toonverse client package.
3. Remove generic book-tool beans from auto-configuration while preserving the classes for the later generic-layer slice.
4. Verify unit behavior and MCP discovery once the reactor can compile the server path.
