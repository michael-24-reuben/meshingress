# Fixes

- Added a reusable source annotation for the shared artifact repository headers and documented the accepted role values.
- Added operation, lifecycle, and response documentation to all artifact repository endpoints.
- Added storage `GET` and `HEAD` documentation, including unsupported byte-range behavior.
- Added Springdoc `/v3/api-docs` regression tests for artifact and storage routes.
