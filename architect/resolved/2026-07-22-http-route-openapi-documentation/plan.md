# Plan

1. Add reusable repository-header documentation and concise operation/response documentation to `ArtifactController`.
2. Document storage file `GET` and `HEAD`, including the unsupported `Range` behavior.
3. Add `/v3/api-docs` assertions for representative artifact and storage contracts.
4. Run the focused OpenAPI tests through the server reactor.
