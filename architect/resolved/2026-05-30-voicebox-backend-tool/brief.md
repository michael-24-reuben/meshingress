# Voicebox Backend Tool Conversion

The repo should become backend-only for this slice and expose `jamiepine/voicebox` as the attached Meshingress MCP tool. The initial implementation keeps the Spring/MCP backend runtime, removes frontend assets from the tracked surface, and replaces the previous bundled demo/social tools with a `toolspace/voicebox` module.
