# Notes

- The temp sample is a design sketch, not directly compilable Java.
- Keep the new module independent from `app/meshingress-server`.
- Reactor root currently targets Java 25 in the live checkout; local verification may need Java 22 compiler overrides if the installed JDK is still 22.
