# Implementation Notes

- 2026-07-21: Implementation was authorized. The live Nextcloud AIO container is healthy on Nextcloud 34.0.0.12 with PHP 8.3.31, and its `custom_apps` directory had no existing custom app source to extend.
- Preserve the pre-existing dirty Meshingress server, audit, progress, storage, and Aegis work. This objective adds only the checked-in Nextcloud app source, targeted Meshingress integration changes if needed, and its own architect lifecycle files.
- Use the disposable account credential only for a live API retry after authentication fails; never record it in source, architect files, logs, or terminal output.
- 2026-07-21: Installed source from `integrations/nextcloud/meshingress` into the live AIO `custom_apps/meshingress` directory and enabled version `0.1.0`. Nextcloud registered all three OCS routes and created `oc_meshingress_import_jobs` through the app migration.
- 2026-07-21: PHP syntax checks passed for all app files. Direct service validation rejected invalid tool identifiers, non-HTTPS URLs, and traversal paths. Full import did not write a file: `admin` has no `Workspace/` folder, so the intended parent-boundary guard rejected initialization before queueing.
- 2026-07-21: The configured historical Meshingress endpoint pointed at a different Tailnet hostname. The actual local AIO route is the `alphasunny11-07` hostname. The disposable `ops` credential was rejected; `occ user:list` shows only `admin` and `temporary`, so no live authenticated OCS success test is possible with it.
- 2026-07-21: Rechecked after the owner reported creating `Workspace/`. Direct filesystem listings and a depth-three read-only search found no folder named `Workspace` under either current user. The intended folder may have been created on a different Nextcloud instance/account or under a different name.
- 2026-07-21: Target correction: the intended host is `alphasunny-xps-8700.tail93ea23.ts.net`, where the owner created `/Workspace` for `ops`, rather than the local AIO instance used for initial installation and validation. OCS authentication as `ops` succeeded, the remote server reports Nextcloud `34.0.0`, and the account belongs to the `admin` group. The local app install remains a test deployment only; it is not the requested production target.
- 2026-07-21: Tailscale reaches the remote Linux host directly. SSH with the current Windows account was rejected with `Permission denied (publickey)`, so deployment still needs an authorized server-side route to copy the custom app under Nextcloud `custom_apps` and run `occ app:enable meshingress`.
- 2026-07-21: Remote WebDAV `PROPFIND` against `remote.php/dav/files/ops/Workspace` returned `207 Multi-Status` and identified the target as a collection. This confirms the requested official workspace parent exists for the authenticated owner account.
- 2026-07-21: Installed and enabled `meshingress` version `0.1.0` on the intended remote Nextcloud 34.0.0.12 instance. `occ router:list meshingress --ocs` verified all three OCS routes and the migration created the app job table.
- 2026-07-21: Live validation exposed and fixed three Nextcloud 34 compatibility defects: use `closeCursor()` instead of unavailable `free()` on database results; bind null update values through the query builder; and support the HTTP client body's string representation as well as stream-like bodies. The background job also accepts array, JSON-string, and object arguments defensively.
- 2026-07-21: The verification import completed for tool `meshingress.verify` at `storage/meshingress.verify/verification/example-4.html`. Worker metadata recorded file ID `1081`, 559 bytes, ETag, MIME type, and SHA-256 `ff67a9d764d6a2367a187734e697f6a53217db9a21c101d410a113ca871a299d`; an authenticated WebDAV GET returned 200 with the same byte count and SHA-256.
- 2026-07-21: The status service returns the completed metadata when executed inside Nextcloud, but the HTTP OCS status route intermittently returns its intentional not-found response immediately after worker execution. The implementation source is current; a controlled Nextcloud web-container reload is required to clear the existing web-process state before closing the record.
- 2026-07-21: Nextcloud 34's own developer release notes use the exact `<nextcloud min-version="34" max-version="34"/>` dependency range. Keep the maximum version until a later major Nextcloud release is explicitly tested; removing it would allow unverified future major versions to load rather than protect against incompatibility.
- 2026-07-21: Added `install-nextcloud-app.js`, an install-only SSH/Docker helper. It requires host/user environment variables, uses the current SSH agent, uploads only to a unique temporary staging directory, refuses to overwrite an existing app directory, syntax-checks PHP, enables the app, prints OCS routes, and removes only its own staging directory. Node syntax, help, and missing-configuration guards passed locally.
- 2026-07-21: Recorded a later Meshingress-server integration: a new storage lifecycle for link-producing tools will submit source links and stable tool identity to this app, then report terminal import metadata. It is deliberately separate from both `EXTERNAL_EXTERNAL` removal and the existing byte-owning local WebDAV handoff.
- 2026-07-21: A protected live installer dry run correctly refused the already-installed remote app and preserved it. The test revealed a POSIX quoting error that had created one nested staging copy under the app directory before stopping; the helper was corrected and that exact verified nested directory was removed. The rerun staged, rejected overwrite, cleaned up, and left no nested copy.
