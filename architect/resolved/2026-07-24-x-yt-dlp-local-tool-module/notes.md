# Notes

The initial wrapper uses typed arguments and `ProcessBuilder`; no shell is invoked. Download paths are calculated beneath `toolspace/x-yt-dlp/runtime/downloads` and rejected if configuration escapes the module root.
