# Summary

`x-yt-dlp` is now a local-only bundled Meshingress tool. It owns its yt-dlp source, JavaScript solver package, provenance, documentation, license material, and runtime output directory; it has no runtime dependency on the separate Dev.py checkout. The MCP surface exposes constrained inspect and download functions, and focused module, server-discovery, and real metadata-only runtime checks passed.
