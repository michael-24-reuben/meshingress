# Brief: Local yt-dlp Tool Module

Create `toolspace/x-yt-dlp` as a local-only Meshingress tool. Its yt-dlp source, JavaScript solver package, dependency notes, and downloaded runtime output must remain inside the module. The result must not refer to the separate Dev.py yt-dlp checkout at runtime.
