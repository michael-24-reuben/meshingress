# Plan

1. Add the ignored local Maven module and its annotation-based Spring auto-configuration.
2. Vendor the yt-dlp and yt-dlp-ejs runtime packages plus provenance and local dependency notes.
3. Attach the module to the reactor and local tool bundle.
4. Verify the module unit test, server tool discovery, and a real local metadata inspection without writing downloads outside the module.
