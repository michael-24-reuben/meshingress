# Summary

This entry is discontinued because the framework usage was removed. Auth will be reimagined as a separate module that owns definitions, framework, and logic, with SQL or JSON backing options, rather than being embedded in `meshingress-server`.

