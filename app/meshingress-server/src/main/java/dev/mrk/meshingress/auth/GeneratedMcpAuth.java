package dev.mrk.meshingress.auth;

public record GeneratedMcpAuth(
        String accessToken,
        String secretKey,
        String authToken,
        String clientId,
        String subject
) {
}
