package dev.mrk.meshingress.controller.roles.registration;

public enum ToolSourceKind {
    LOCAL_JAR,
    MAVEN_COORDINATES,
    CLASSPATH_BUNDLE,
    MAVEN_BUNDLE,
    SERVER_NATIVE,
    PUBLICATION_RECORD
}

